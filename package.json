{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "eslint --fix .",
    "bootstrap": "lerna bootstrap --force-local",
    "compile": "lerna run compile",
    "test": "lerna run test --stream",
    "test:integration": "lerna run test:integration --stream"
  },
  "workspaces": [
    "packages/example",
    "packages/puzzlescript",
    "packages/puzzlescript-cli",
    "packages/puzzlescript-web"
  ],
  "devDependencies": {
    "@types/figlet": "*",
    "eslint": "*",
    "eslint-config-prettier": "*",
    "eslint-config-standard": "*",
    "lerna": "*",
    "typescript-eslint": "*",
    "yarn": "*"
  }
}
